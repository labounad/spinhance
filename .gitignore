# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg

# Environments
.env
.venv
env/
venv/
*.conda
*.mamba

# Jupyter
.ipynb_checkpoints/
*.ipynb

# ── Large data files — live in S3 (s3://spinhance-data/), not in git ──────────

# Raw source datasets (re-download from ChEMBL / PubChem as needed)
generate/chembl/

# Computed pipeline outputs
generate/data/*.csv
generate/data/*.gz
generate/data/buckets/

# Spin-system JSON outputs (large ones are in S3; spin_systems.json is kept)
mol_to_spin_system/data/spin_systems_chembl.json
mol_to_spin_system/data/spin_systems_chembl_8spin_randomized.json
mol_to_spin_system/data/spin_systems_pubchem.json.tar.gz
mol_to_spin_system/data/buckets/

# Generated spectra (regenerate from spin_systems.json + simulation CLI)
simulation/data/spectra/

# MNova batch outputs
simulation/mnova_outputs/

# ── ML artifacts — stored in S3 sessions (s3://spinhance-data/training/) ───────
model/checkpoints/
model/runs/
*.pt
*.pth
*.ckpt

# Smoke test output PNGs
smoke_*.png

# ── Misc ───────────────────────────────────────────────────────────────────────
outputs/
*.sdf
*.mol2
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
simulation/examples/*.cmp_*.png
modelv2/runs/

# Large trained checkpoints pulled from S3 (not tracked)
model_artifacts/
